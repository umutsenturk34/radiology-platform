# Public backend base URL, including /api/v1. Never add secrets here.
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# Public Socket.IO endpoint. The backend handles authentication.
NEXT_PUBLIC_WS_URL=http://localhost:3001
